# Repo Conventions

- Markdown files are operational and directly reusable.
- JSON uses snake_case IDs and stable arrays for deterministic parsing.
- Substyle names are canonical and should not be renamed downstream.
- Drift warnings are mandatory in every stylistic subsystem.
- “Too clean” is treated as a quality failure.
