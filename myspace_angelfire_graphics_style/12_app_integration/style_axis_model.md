# Style Axis Model

Represent style as multidimensional vector using the primary knobs. For mixed outputs, compute weighted blend but preserve substyle-specific constraints (e.g., widget ecology when Angelfire weight > 0.6).
